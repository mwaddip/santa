[package]
name = "scoreboard"
version = "0.1.0"
edition = "2021"
description = "SANTA conformance scoreboard generator: .santa/results.json -> per-conformer shields.io badge JSONs + an index.html dashboard, published to GitHub Pages."

[dependencies]
serde_json = "1"
